# ==============================
# COMMON CONFIGURATION
# ==============================
server.port=${PORT:8080}
server.servlet.context-path=/sghris

# Logging
logging.level.org.atmosphere=warn
spring.mustache.check-template-location=false

# Launch browser automatically (only for dev)
vaadin.launch-browser=false

# Vaadin configuration
vaadin.allowed-packages=com.vaadin,org.vaadin,com.vaadin.componentfactory,io.softwaregarage.hris

# JPA initialization
spring.jpa.defer-datasource-initialization=true

# ==============================
# EMAIL SETTINGS
# (replace with real credentials in environment variables)
# ==============================
spring.mail.host=smtp.mail.yahoo.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:gdpags5}
spring.mail.password=${MAIL_PASSWORD:qqejtekdrguskbuu}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ==============================
# ACTIVE PROFILE
# ==============================
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}
